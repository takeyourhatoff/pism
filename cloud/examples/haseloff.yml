job_name: haseloff-smoke-001

inputs:
  base: haseloff

pism:
  executable: pism
  args: >
    -i {{INPUTS.base}}/startSMALLablate.nc
    -bootstrap
    -Lz 3500
    -Mx 51
    -My 51
    -Mz 61
    -y 1
    -no_temp
    -o {{OUTPUT_DIR}}/out-{{JOB_NAME}}.nc
